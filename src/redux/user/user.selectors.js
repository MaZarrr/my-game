// import { createSelector } from 'reselect';
// const selectPlayer = state =>  state.player;

// export const buttonCheckedIncSelector = (state) => {
	
// 	if(state.user.activeParametrs === "") {
// 		return
// 	}

// 	if(state.user.activeParametr !== "") {
// 		console.log(state.user.basicParameters[state.user.activeParametrs]);
// 		return state.user.basicParameters[state.user.activeParametrs]

// 	}
// }

// export const buttonCheckedDeskSelector = (state) => {

// 	if(state.user.activeParametrs === "") {
// 		return
// 	}
// 	if(state.user.activeParametrs !== "") {
// 		return state.user.basicParameters[state.user.activeParametrs]

// 	}
// }